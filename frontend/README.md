# NuxtJS storefront

Ứng dụng này là frontend khách hàng độc lập với Vue Admin.

- Storefront: `/`
- Admin: `/admin/`
- Public API: `/api/v1`
- Admin API: `/api/admin/v1`

Trong development, không truy cập trực tiếp port 3000. Hãy đi qua Nginx Gateway để giữ đúng same-origin.
